// This is the entry point of our React app - it's the first JavaScript file that runs
// Think of it as the ignition key that starts up our entire fitness platform
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';  // Our main app component
import './index.css';     // Global styles that apply to everything
